/**
 * CRTOverlay Component
 *
 * Provides vintage CRT monitor effects across the entire application:
 * - Animated scanlines
 * - Vignette effect
 * - Fixed position overlay that doesn't interfere with interactions
 */

export default function CRTOverlay() {
  return <div className="crt-overlay" />;
}
